version: 0.1
frontend:
  phases:
    preBuild:
      commands:
        - nvm install $(cat .nvmrc)
        - npm ci
    test:
      commands:
        - npm test
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: build
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
