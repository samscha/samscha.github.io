import {
  faSpinner,
  faCircleNotch,
  faCode,
  faEnvelopeSquare,
  fas,
} from '@fortawesome/free-solid-svg-icons';
import { fab } from '@fortawesome/free-brands-svg-icons';

const forts = [faSpinner, faCircleNotch, faCode, faEnvelopeSquare, fas, fab];

export default forts;
