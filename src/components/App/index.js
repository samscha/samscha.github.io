import { Route, Routes } from 'react-router-dom';
import { About, Contact, Four04, Home } from '../../pages';
import App from './App';
import init from './init';

export default App({ About, Contact, Four04, Home, Route, Routes, init });
